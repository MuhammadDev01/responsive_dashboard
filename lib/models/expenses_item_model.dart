class ExpensesItemModel {
  final String avatarImage, title, date, price;

  ExpensesItemModel(
      {required this.avatarImage,
      required this.title,
      required this.date,
      required this.price});
}
